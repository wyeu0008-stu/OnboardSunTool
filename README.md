# OnboardSunTool
For monash FIT5120 onboarding project

## Package

Run the packaging script to generate a distributable zip file:

```bash
./scripts/package.sh
```

Output:
- `dist/page1-framework-ver1.zip`
